MODULES := pingsrv pingcli
#utcli utsrv
pingsrv-objs := ping_srv.o

ifeq ($(PATCHLEVEL),6)
pingcli-objs := ping_cli.o
#utcli-objs := ut_cli.o
#utsrv-objs := ut_srv.o
else
ping%.c: ping_%.c
	ln -sf $< $@
endif

@INCLUDE_RULES@
